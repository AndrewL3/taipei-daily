{
  "name": "ntpc-garbage-tracker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@tracker/config": "workspace:*",
    "turbo": "^2",
    "typescript": "^5.7",
    "prettier": "^3"
  },
  "pnpm": {
    "overrides": {
      "drizzle-orm": "^0.38"
    }
  }
}
